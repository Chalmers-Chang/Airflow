# Optional custom image. The official docker-compose.yaml uses
# apache/airflow:2.10.5-python3.8 by default (see .env).
#
# To build this file instead of pulling the Hub image:
#   1. In docker-compose.yaml, comment `image:` and uncomment `build: .`
#   2. docker compose build
#   3. docker compose up
#
# Extra Python packages are pinned by uv in the repo-root pyproject.toml.
FROM apache/airflow:2.10.5-python3.8
